"use strict"

const NotificationMessages = require("./notification-messages.model")
const NotificationTemplates = require("./notification-templates.model")

module.exports = {
    NotificationMessages,
    NotificationTemplates
}
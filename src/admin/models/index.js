"use strict"

const User = require("./user.model")
const UserPassword = require("./user-password.model")
const Role = require("./role.model")
const UserRoles = require("./user-roles.model")

module.exports = {
    User, UserPassword, Role, UserRoles
}